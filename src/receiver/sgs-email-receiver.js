module.exports = (function () {
	'use strict';

	function SGSEmailReceiver () {}

	SGSEmailReceiver.prototype.init = function (config) {
		config = config || {};
	};

	SGSEmailSender.prototype.receive = function (data, callback) {

	};

	return new SGSEmailReceiver();
});
